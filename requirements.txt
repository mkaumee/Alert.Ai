# AlertAI Minimal Requirements for Railway Deployment
# Core Flask dependencies only
Flask==3.0.0
requests==2.31.0
flask-cors==4.0.0
python-dotenv==1.0.0
gunicorn==21.2.0

# Google Gemini - try without version constraint
google-genai

# Twilio for WhatsApp
twilio==8.10.0

# Essential build tools
setuptools
wheel
pip